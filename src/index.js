import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return (
    <div>
        <h2>Haiya! We are ready to roll...</h2>
    </div>
    );
}

ReactDOM.render(<App />, document.querySelector('.container'));
